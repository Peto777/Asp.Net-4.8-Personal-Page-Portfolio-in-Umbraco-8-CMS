@model PeterGlozikUmbracoOsobnaStranka.lib.Models.DashboardBlog.CountryModel

@{
    using (Html.BeginUmbracoForm<PeterGlozikUmbracoOsobnaStranka.lib.Controllers.DashboardBlog.CountryController>("SaveRecord", null, new { @class = "form-horizontal", role = "form" }))
    {
        <div class="osobnastranka-form">
            <h1>KRAJINA</h1>
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true, "Údaje sa nepodarilo uložiť. Prosím opravte chyby a skúste to znovu.")
            <div class="row">
                <div class="col-md-4">
                    <div class="form-item-hidden">
                        @Html.TextBoxFor(m => m.pk, new { @class = "form-control", @readonly = "readonly" })
                    </div>
                    <div class="form-item">
                        @Html.LabelFor(x => x.Code)
                        @Html.TextBoxFor(m => m.Code, new { @class = "form-control", })
                        @Html.ValidationMessageFor(m => m.Code)
                    </div>
                </div>
                <div class="col-md-8">
                    <div class="form-item">
                        @Html.LabelFor(x => x.Name)
                        @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.Name)
                    </div>
                </div>
            </div>
            <div class="row new-section">
                <div class="col-md-12">
                    <a href="#" onclick="document.getElementById('submitButton').click(); return false;" class="btn btn-default btn-lg">Zapísať</a>
                    <a href="@Url.Content("~/sk/clenska-sekcia/krajiny")" class="btn btn-default btn-lg">Späť</a>
                    <input type="submit" id="submitButton" class="hidden" />
                </div>
            </div>
        </div>
    }
}
